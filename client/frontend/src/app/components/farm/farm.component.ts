import { Component }    from '@angular/core';
import { AppComponent } from "../../app.component";

@Component({
  moduleId: module.id,
  selector: 'farm',
  templateUrl: 'farm.component.html'
})
export class FarmComponent extends AppComponent {
}
