import { Component }    from '@angular/core';
import { AppComponent } from "../../app.component";

@Component({
  moduleId: module.id,
  selector: 'public',
  templateUrl: 'public.component.html'
})
export class PublicComponent extends AppComponent {
}
